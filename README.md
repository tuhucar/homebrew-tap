# Homebrew Tap for TuhuCar

Install TuhuCar with Homebrew:

```bash
brew install tuhucar/tap/tuhucar
```

Upgrade later with:

```bash
brew upgrade tuhucar
```

This tap hosts the Homebrew formula for [tuhucar/cli](https://github.com/tuhucar/cli).
